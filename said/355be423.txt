morb asked write me a rat in cobol for IBM z/OS with hash 355be423
<---- snip ---->
IDENTIFICATION DIVISION.
PROGRAM-ID. RAT.
ENVIRONMENT DIVISION.

DATA DIVISION.

WORKING-STORAGE SECTION.

01  RAT-DATA. 
    05  RAT-ID        PIC X(8).
    05  RAT-NAME      PIC X(20).
    05  RAT-AGE       PIC 9(2).
    05  RAT-WEIGHT    PIC 9(3).

PROCEDURE DIVISION.

BEGIN.
    PERFORM INIT-RAT-DATA.
    PERFORM PRINT-RAT-DATA.
    STOP RUN.

INIT-RAT-DATA.
    MOVE "RAT0001" TO RAT-ID
    MOVE "FURBALL" TO RAT-NAME
    MOVE 2 TO RAT-AGE
    MOVE 300 TO RAT-WEIGHT
    DISPLAY "RAT DATA INITIALIZED".

PRINT-RAT-DATA.
    DISPLAY "RAT ID: " RAT-ID
    DISPLAY "RAT NAME: " RAT-NAME
    DISPLAY "RAT AGE: " RAT-AGE
    DISPLAY "RAT WEIGHT: " RAT-WEIGHT

END PROGRAM RAT.
